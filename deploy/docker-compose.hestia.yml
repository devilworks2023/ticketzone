version: '3.8'

services:
  ticketzone:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ticketzone-web
    restart: unless-stopped
    ports:
      - "3000:80"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 3s
      retries: 3
